=== 1. JVM’s role in thread management ===
Well. Hello hello hello my friends. Welcome back. So in this video we need to talk about the role of the Java virtual machine in thread management. JVM, the Java virtual machine. It's actually the heart of Java's runtime environment. Now, when it comes to threads, the JVM plays a pivotal role in creating scheduling and managing threads efficiently. So we're going to dive right in to how the JVM handles threads. We'll first need to address key responsibilities of JVM and thread management. So you've got your thread lifecycle management right. So the JVM manages the lifecycle of threads from their creation through their termination. Now this is going to include new. The thread is created but not yet started. Runnable thread is ready to run but waiting for CPU time. Running Threat is actively executing its task. Blocked waiting or timed waiting. The threat is waiting for a resource or a signal terminated. Thread has completed its task. Well done thread, so the JVM ensures that threads transition smoothly through these states. Okay. We also need to look at mapping to operating system threads. Now Java threads are mapped to native OS threads. When a thread is created in Java, the JVM requests the operating system to create a corresponding OS thread. The operating system scheduler then determines how these threads are executed on the CPU cores. Context switching. So the JVM relies on the operating system to perform context switching between threads. What this does is allows multiple threads to share CPU time without any conflict, creating the illusion of parallelism. Thread prioritization. So each thread in Java has a priority. Default is norm priority. The JVM uses this priority as a hint to the OS scheduler, which influences the order of thread execution. Thread synchronization. The JVM provides mechanisms like synchronized wait and notify, allowing threads to coordinate their actions. This ensures consistency in shared resources. Also garbage collection threads. So the JVM itself uses threads for internal tasks such as garbage collection. These threads run in the background, ensuring memory management without halting the application. I3 Forkjoinpool and parallelism. Now, starting with Java seven, the JVM also manages Forkjoinpool. A thread pool optimized for parallel computing. It uses a work stealing algorithm for efficient thread utilization. Now let's have a look at what JVM does when optimizing multithreading. Balancing threads across available processors. Minimizing overhead through efficient context switching, allowing developers to focus on business logic while it handles thread level complexity. Sounds good. So now that you know that, let's move on over to IntelliJ and get some practice in. All right. So we're going to make an example that looks something like this. We're going to create multiple threads in a Java program and observe how the JVM manages their execution. Each thread will print its name and current state to demonstrate the thread life cycle. All right, let's take that into action. So first I'll create a new Java project named JVM and threads. In this project. First, I'll create a custom runnable class that simulates a task for the threads. Each thread will log its name and state. So let's just create this class right now and its name. Yeah, let's just make it a custom task. This class will implement the runnable functional interface. Now of course we'll need to override the run method. So in the run method I'll print the current thread name and its current state. Okay, so that class is now ready. Let's go ahead and create the main test class named JVM Thread Management Test. And also I'll add the main method. So next we can create and start three different threads assigning the custom task to each of them. Now, in addition to this code, we'll log the status of each thread before and after starting them, just to observe the transitions in their life cycle. All right. Yeah. Let's run the code. And we can have a look at the output before start. The threads are in the new state, right. Because they've been created, but they have not started yet. During execution, once started, each thread enters the runnable state and executes its task. When a thread start method is invoked, it transitions from the new state to runnable. This means that the thread is ready to execute, but may not be running yet. So in Java, runnable includes both actively running threads and those waiting for CPU time queued by the operating system scheduler. Right. So the threads are marked as runnable after start method, but you might have expected it to be terminated. Right. Let's talk about that for a minute. So we've got the JVM that's handed off the threads to the operating system for scheduling. The OS may not immediately assign CPU time to the thread, so they remain in the runnable state. All right. So the main thread checks the states after giving enough time for the threads to finish execution. So if your main thread runs too quickly, it might log the states before the threads complete their tasks. And the last output is thread name and state. So these lines appear after the main thread prints the states. Because the main thread does not wait for the child threads to execute. The runnable state here shows that each thread is in the pool of threads eligible for execution. Now let's try to get to the terminated state in the console. In order to see the threads transition to the terminated state, we can just add a small delay in the main thread. The code we write now will give the child threads time to complete their execution. After the start methods, I'll add a half second delay. This delay will be enough time for the threads to complete their work. So this modification should allow the main thread to log the terminated states after the child threads finish. Let's run the code again. Well, now you can see we've got the terminated state this time. Perfectamundo are my friends. What do we do here? So we have just explored Jvms role in thread management. Some key takeaways for you. How the JVM handles thread lifecycles and maps them to OS threads. The importance of efficient context switching and synchronization. Practical demonstration of thread states and transitions. Runnable state indicates threads are ready to run, but not necessarily running. Main thread versus child threads. The main thread doesn't wait for child threads by default, so it may log states before they change and synchronization. Introducing delays like thread.sleep can help observe state transitions more clearly. All right my friends, that's it for now. Let's take a short break here and I'll see you next time.

=== 2. Thread priorities ===
Well, how do my friends welcome back. So check it out. We need to talk about the priorities of threads in Java. Every thread has a priority. It helps the operating system decide the order in which threads should execute. Right now, priorities are represented as integer values ranging from 1 to 10. The value of the minimum priority is one and the value of maximum prior is ten. Okay. The default value though is five, and that's defined as normal priority. Thread priority in Java serves as a hint to the thread scheduler, which determines the execution order of threads. However, it's not a guarantee of execution order due to differences in operating system implementations of thread scheduling. So some of the key points to keep in mind about your thread priorities will include preemptive multitasking, so a higher priority. Threads are given preference for execution over lower priority threads provided that they are ready to run starvation risk. So if many high priority threads are running, lower priority threads may not get enough CPU time leading to starvation. Fairness mechanisms. Now, modern thread schedulers use mechanisms like time slicing to ensure fairness, allowing lower priority threads some CPU time changing priorities so thread priorities can be set using the set priority method. The priority can be queried using the get priority method, but it is important to note here thread priorities need to be used cautiously. Overreliance on priorities can make the program behave inconsistently across different platforms. All right, now that we've had that nice chat, Let's go to IntelliJ and get some practice in. So here we're going to create an example to demonstrate thread priorities in action. So we'll need to create three threads with different priorities. Each thread will print a series of numbers. So the goal here is just to observe how the priority affects thread scheduling. Let's take our actions. So I want to create a new Java class in the JVM and threads package. Let the name of the class be thread priority example. And I'll just add the main method. Now in the main method I need to create three threads representing the minimum, normal and maximum priorities of threads. So each thread will print numbers from 1 to 5 to the console with its name and priority. All right, let's create the threads. All right. So once the threads are ready now let's set priorities to these threads using the set priority method. And finally we start the threads and observe how the priorities influence execution. All right. Let's go ahead and run the code. And now observe the output. Of course, the output may vary due to the operating system's thread scheduling policies, but a common pattern might just go like this. The high priority thread gets preference and execution and completes first. Medium priority thread executes next after the high priority thread finishes and the low priority thread executes last because, well, it's got the lowest priority. Now, on systems with time slicing or fairness mechanisms, the threads may interleave execution. Okay, now let's just review what we've been through here. Thread priorities provide a great way to hint to the scheduler about the importance of threads, while higher priority threads may execute earlier. The actual behavior depends on the operating system and the JVM implementation. Okay, so my friends, why don't we take a short break here, digest all that, and I'll see you in the next video.

=== 3. Daemon threads ===
Well. Hello there, my friends. Welcome back. So we need to talk about daemon threads. Yeah. First off let's understand what daemon threads actually are. So in Java threads are categorized as either user threads or daemon threads. Now both types of threads run concurrently, but they differ in their role in behavior. First up let's talk about user threads. Now the user threads are just regular threads that perform tasks and keep the JVM running as long as they're alive. The JVM does not terminate until all user threads have completed. Now for the daemon threads. The daemon threads are actually low priority threads designed to provide background services or support tasks for user threads. So the JVM terminates automatically if only daemon threads remain. Common example of a daemon thread is a garbage collector in Java. It runs in the background of free unused memory without blocking the execution of the main program. Let's have a look at basic features of daemon thread. So daemon threads are set using the set daemon method, and they must be set as daemon threads before starting. Otherwise an illegal thread state exception is thrown. Daemon threads are actually ideal for tasks like monitoring, cleanup, or even periodic operations. All right, so let's move on to IntelliJ and get some practice in. So here we will create an example to demonstrate the behavior of daemon threads. So the example is going to go like this. We'll create a user thread and a daemon thread. The user thread will perform a simple task while the daemon thread will print messages in the background. So we're going to observe how the daemon thread stops running as soon as the user thread completes. So why don't we get to it? I'll first need to create a new Java class named Daemon thread. Example. Also, I'll add the main method. So I need to define a user thread and this user thread will perform a simple task. For example, let's say that it prints numbers from 1 to 5 to the console. All right, so we'll just create this simple thread. After printing the number of the console. Let's delay the thread by one second. And this is just to simulate delayed execution. And lastly, let me define a name for the thread. So let's just call it user thread. All right so now the user thread is ready. Let's define a daemon thread. Now this thread will run in the background and print a message every second. So let's create it. And here I'll create an infinite loop with a while loop. We'll let the message be. Daemon thread is running. Okay, so I'll need to delay it. And its name will be Daemon Thread. So this thread is ready to go. But if we leave it like this, there's no difference between it and the normal thread. Remember the user thread? So we need to actually identify it as a daemon thread. That means we'll need to use the set daemon method. I'll pass the value true as a parameter. So now we can finally start both threads. Let's run the code and observe the output. So the likely output will be this. After the user thread completes, the daemon thread stops immediately. The program will terminate. Right. So the user thread executes its task printing numbers from 1 to 5. The daemon thread runs in the background, continuously printing its message. When the user thread completes, the JVM terminates, stopping the daemon thread automatically. All right, my friends, there it is. So let's just summarize what we've been doing here. Daemon threads provide essential background services without preventing JVM termination. They're very useful for tasks like logging, monitoring, or clean up operations. However, since they terminate when user threads complete, critical tasks should not rely solely on daemon threads. All right, my friends, there you have it. Let's take a short break here. I'll see you next time.
