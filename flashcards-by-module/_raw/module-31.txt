=== 1. Mock Exam-Java SE 21 Developer Professional Certification (Exam Number 1Z0-830) ===
Well. Hello there, my friends. Welcome, welcome welcome back. Guess what? We've now finished the topics in our course. So that means in this video we're going to solve a practice exam together. And this will reinforce the topics that we've learned and prepare you for the Java SE 21 developer exam. Yeah. So we've chosen a few questions that we'll solve together in this video. From the questions that we've seen historically in previous exams, as well as a type of questions that are likely to be encountered by you in future exams, of course it's going to be useful, so just focus, get ready and let's get to it. Question number one. So this question is about the history of Java. So similar questions will be encountered and they'll just vary in Scope, but I just want you to see this question and go ahead and pause the video here and try to solve it yourself. I'm not going to show you the answer until the next slide. All right. So if you chose option B you answered correctly. C Java was developed by James Gosling, Mike Sheridan, and Patrick Norton at Sun Microsystems way back in the early 1990s. Now, the language was initially called oak, and it was intended for interactive television. However, the technology was way too advanced for the digital cable industry at the time, so the team repurposed Java for internet applications. That's forward thinking. So A is going to be incorrect because Java was developed by Sun Microsystems, not Microsoft. C is incorrect because, well, the first version of Java was Java 1.0, not JDK 2.0. D is incorrect because Java was designed as an object oriented programming language. E is incorrect because Java was designed to be platform independent. Write once, run anywhere. F is incorrect because Java was not meant for low level system programming, but for cross-platform application development. Therefore, my friends, the answer is option B. All right, so why don't we move on to the next question. Question number two. This question is about the JVM. So again why don't you pause here and try to solve it yourself. We're not going to show you the answer until the next slide. Yeah. So if you chose option C you answered correctly JVM or the Java Virtual Machine is responsible for Before executing Java bytecode, which is produced after compiling a Java program. Now, the main advantage of the JVM is that it allows Java applications to be platform independent. Now remember the Wora write once, run anywhere. A is actually incorrect because the JVM does not translate Java source code into machine code directly. Instead, it executes bytecode which is generated by the compiler. B is incorrect because Java programs are not converted into C plus plus code. Well, at any point D is incorrect because the JVM is required to run Java applications not just for development. E is incorrect because the JDK includes the JVM and JRE, but the JVM itself does not include the JDK. Important distinction F is incorrect because Java programs can't even run without the JVM. All right, why don't we move on to the next question? Question number three. So this question revolves around Java variables scope shadowing. So again pause the video here. Try to solve it yourself. Give it a real good go. Okay. We won't give you the answer of course till the next slide. All right. So if you chose option D yes you answered correctly. This code will not compile because of variable shadowing. The issue occurs inside the block inside the main method. A local variable x has already been declared in the main method. Then inside the inner block there's another attempt to declare int x equals 20. Now, Java does not allow redeclaring a local variable in the same scope or a nested scope. So the compiler is going to throw an error. Okay, so if we were to remove the inner int x equals 20 and replace it with x equals 20, the program will print 20. All right let's move on. Next question. Question number four. This question is about logical operators and short circuiting. Again go ahead pause the video here. Try to solve it yourself. We'll give you the answer on the next slide. All right. So if you chose option B you answer correctly since x equals five. The first condition that is x is greater than two is true. So the second condition that is plus plus x is less than ten is never evaluated due to short circuiting or operator. As a result, x remains five because plus plus x is never even executed. So the output will be five, not six. See, even if we were to replace the Or operator with bitwise Or, which does not short circuit, then plus plus x would execute and then the output would be six. Good thinking. All right. Let's move on to the next question. Question number five. This question is all about the switch case statement. A variable of type int is given. And this variable is controlled by the switch case. The question is what will be the output of this code. Go ahead pause the video. Try to solve it yourself. We'll share the answer with you on the next slide. Yeah, so your answer is correct if you chose option F because this code does not compile. So why doesn't this code compile? Yeah. Let's have a look at it now. So you got to notice that the first case uses three minus one. So this actually means case two okay. Next case two is used again. Therefore the same case gets used twice. Therefore a compilation error occurs for cases. And this code cannot be compiled. Yeah. Let's move on to the next question. Question number six. All right. So this question is about the multidimensional arrays. Go ahead pause the video. Try to solve it yourself. And we'll share with you the answer on the next slide. Yeah. So your answer is correct. If you chose option F, Java allows jagged arrays, that is rows of different lengths, so there's no problem. However, we are trying to access the third element of the second one dimensional array. But you see the second one dimensional array has two elements. So this will throw the index out of bound exception not the compilation error. Good good good. Let's move on to the next question. Question number seven. All right. So this question is about the foreach loop and array. Just go ahead and pause the video and try to solve it yourself. We'll share with you the answer on the next slide. Yes indeed. Your answer is correct. If you chose option A for each loops does not modify the original array C, the variable num is a copy of the array element, not a reference to the original value. NUM. Multiply and assignment two only modifies a copy, not the actual array element. The array remains unchanged, so the element at index two is still three. All right, let's move on to the next question. Question number eight. This question is about the method overloading. Again go ahead pause the video. Try to solve it yourself and we'll reveal the answer on the next slide. Yes your answer is correct. If you chose option a c Java prioritizes exact matches first, and the print method with two int parameters is an exact match. For print five and ten, print integer nums is a var args method, which is considered only if no exact match gets found. Since five and ten are primitive int types, they directly match with the method print method with two end parameters. Now, if we were to remove the print method with two int parameters, then the print method with the var args parameter will be called. Let's move on to the next question. Question number nine. This question is about the method overloading. So just pause the video. Try to solve it yourself, and we'll share with you the answer on the next slide. So my friends your answer is correct. If you chose option D, see the print with parameters five and ten is ambiguous because the print method with parameters int and double is a valid match because ten is int and INT can be widened to double. The print method with parameters double and int is also a valid match, because int five can be widened to double, and in this case Java cannot decide which method to choose because both require widening conversions, causing a complication error. All right, let's move on to the next question, which happens to be question number ten. So this question is about operators. It asks which of the following operators can be used with the boolean data type. Go ahead and pause the video here and try to answer it yourself. And we'll share the answer on the next slide. Yes you have given the correct answer. If you have selected options B, F, G, and h, you see the operator in option A is the unary increment Operator. You cannot apply it to boolean data types, can you? The operator in option B is the equal to operator. This operator compares two different types. Returns true if same false otherwise. So it can be used with boolean type. The operator and option C is the add and assignment operator not used with a boolean data type. The operator and option D is the subtraction operator. This is also not going to be used with the boolean data type. The operator and option E is the greater than or equal to operator. This is also not used with the boolean data type. What about the operator in option f? This is the not equal to operator. This operator compares two different types. Returns false if the same not true. It can be used with a boolean data type. The operator in option G is the conditional and operator. This compares two Boolean types. If both are true, the result is true. Otherwise it is false, so it is used with the boolean data type. The operator in option h is the logical complement operator. It makes false the value of true, and it makes true the value of false. It's definitely used with a boolean data type. So as a result the answer is options b, f, g and h. Way to think your way through it. Let's move on to the next question. Here we are. Question 11. And this question is about the method overloading. So why don't you pause the video again and try to solve it yourself. And we'll share with you the answer on the next slide. Yes. So your answer is correct. If you chose options A, D and E. So option A static import is valid only for static methods or static variables in other classes. Option D show message method must be static, otherwise it cannot be imported with static import. Option E private methods can be accessed within the same class. So these are correct, but the other options are incorrect. So let's have a look. Option B since the increase method is not static, we cannot access it in the main method. We can access it, but we have to create an instance of the class app and use this object to access the non-static fields. Option C. Well, since the field count is static, we can access it in the main method without using the instance of the class app. We can access it directly. Option E. The program does not throw any exception. All right, let's move on to the next question. Question 12. So this question is about the constructor. Go ahead pause the video. Try to solve it yourself. And we'll share the answer with you on the next slide. All right so your answer is correct. If you chose option B, the constructor test with one parameter is called in the main method with parameter ten and the. This call calls the default constructor, which prints default constructor x equals five. And lastly, this dot x refers to the static field, not the parameter x, because we haven't yet transferred the parameter x to the variable x of this class in the constructor. So the. This dot x refers to the five, not ten. All right. Let's move on to the next question. Lucky question number 13. Now this question is about the sealed classes. So go ahead and pause the video. Try to solve it yourself and you'll see the answer on the next slide. Okay. So your answer is correct. If you chose option C the sealed keyword restricts which classes can extend animal. So this is correct. The class dog is non sealed meaning it can be extended. The class cat is final meaning it cannot be extended. The program will compile successfully. There's no compilation error. Now if cat were sealed, it must specify permitted subclasses. Classes. All right, let's move on to the next question. Question 14. This question is about the record classes. Go ahead. Pause the video. Try to solve it yourself. And of course you'll see the correct answer on the next slide. Now if your answer happens to be option A you are correct. If you notice the class keyword is used after the record keyword. This will cause a compilation error. If you want to make this class a record class, you're not going to need to use the classes keyword here. Just using the record keyword will be enough. Therefore, this code will cause a compilation error if the definition was made correctly. That is, if student was a record class. This time the code would compile successfully and when you execute the code, we would see the creating student John output in the console. Now remember just once again records automatically generate constructor getters equals method, hashCode method, and toString method. The compact constructor does not need explicit field assignments as they are set automatically. All right, let's move on to the next question. Question 15. This question is about abstract classes and inheritance. Go ahead pause the video. Try to solve it yourself. We'll share with you the answer on the next slide. Okay. Your answer is correct if you chose option A. But why is that? Firstly, this code compiles as it is. There is no compilation error because the abstract classes can have constructors. This code does not throw a runtime exception. So why does the output first print the animals constructor and then the cats constructor? In the main method, when creating an object from the cat class, the constructor of the cat class is executed. However, in this constructor, the super call command automatically calls the constructor of class A. In other words, when the constructor of the cat class is called with the new cat statement, the constructor of the animal class is called first. Since this class is derived from the animal class, then the constructor of the cat class is called. This follows the rule that constructors of parent classes are called before child class constructors. All right, let's move on to the next question. Question 16. Okay. So this question is about the interfaces. Go ahead pause the video. Try to solve it yourself. And we'll be sharing with you the answer on the next slide. All right. So your answer is correct. If you chose option C, D and F. Let's go through it step by step for a great explanation. Option A is going to be wrong because interfaces can have default methods. With Java eight, default methods have been added to interfaces. A method can have a body and an interface by using the default keyword. This allows classes implementing the interface to use this method without necessarily overriding it. Option B is wrong because default methods do not have to be overridden. Default methods are optional, meaning that it is not mandatory to override them. A class can use the default method in the interface, or override it if it wants to. Option C is right It because with Java nine, private static methods can be used in interfaces. Private static methods can only be called within the same interface. Option D is also right because Java does not support multiple inheritance, but interfaces do. A class can implement more than one interface. Now we cannot accept option E is correct because well, except for default and static methods in the interface. All other methods are automatically considered public, so there is an exception here. If a method in the interface is not default or static, it is automatically public abstract. However, this rule is not valid for default and static methods because default methods must be public, but they are not abstract. Static methods can be public, but they are not abstract. Option F is correct because static methods defined in the interface cannot be overridden because static methods do not belong to a class or object, but to the interface they are connected to. All right, you look ready to move on to the next question. Question number 17. This question is about the threads. So go ahead and pause the video. Try to solve it yourself. And we'll be waiting with you on the next slide. If you want to know the answer. All right so your answer is correct. If you chose option D let's explain it step by step. Though option A is going to be incorrect because a thread can be made to run again while it's in the waiting state by methods such as notify or interrupt. However, this operation does not throw an illegal state exception In a legal state. Exception usually occurs when the current state of an object is invalid for the operation being performed. However, it is a valid operation for a thread to switch from waiting to running. Option B is incorrect because a thread does not use the CPU while it is in the waiting state. The waiting state is a state in which a thread is completely stopped working, and cannot continue working without a lock being released, or a notification notify being received. So the CPU only executes threads that are in the runnable or running state. Option C is incorrect because when the timed waiting period expires, the thread does not go to the terminated state. Instead, it becomes runnable again and waits for execution by the processor. The terminated state means that the thread has completely finished working and will not run again. Option D is correct. If a thread is in waiting or timed waiting state and the interrupt method is called by another thread, an interrupted exception is thrown. Therefore, a thread in waiting state must handle the interrupted exception. Otherwise a compilation error will occur. All right, let's move on to the next question. Question number 18. This question is about the zoned date time. So go ahead pause the video. Try to solve it yourself, and we'll share the answer with you on the next slide. Well well well your answer is correct. If you chose option C. Yeah. Let's explain it step by step. Daylight savings time DST. This is the practice of moving clocks forward by one hour in spring and setting them back by one hour in autumn. Now in Chicago. DST ended on November 7th, 2021 at 2 a.m., meaning that the clocks were set back one hour, effectively repeating the hour from 1 a.m. to 2 a.m.. Now, let's just analyze the given Java code step by step. So the code initializes a zoned DateTime object for November 7th, 2021 at 1:30 a.m. in America Chicago Time Zone. Now this falls within the hour that is repeated due to the DST transition, right? So after initializing, it adds one hour to the zoned DateTime object. So normally adding one hour to 1:30 a.m. would result in 2:30 a.m.. However, since DST ends at 2 a.m. clocks are moved back to 1 a.m., meaning that the hour from 1 a.m. to 2 a.m. repeats. As a result, the object an hour later also represents 1:30 a.m., but in standard time that would be Central Standard Time instead of Central Daylight Time. All right, so now we can have a look at the first print statement since an hour later falls into the repeated hour. It's our value remains one. So the first print method prints true. Now let's have a look at the second print statement. The zoned datetime object was created during daylight savings time, while the object and hour later is now in standard time. Since the time offset changes from UTC five to UTC minus six, the comparison returns false. Thus and therefore. The correct option is C true false. And no, it's not a trick question. It's just something that you got to work through the logic. Now let's move on to the next question. Question 19. This question is about the date and time. Go ahead and pause the video. Try to solve it yourself, and we'll share the answer with you on the next slide. All right. So your answer is correct. If you chose option A let's explain it step by step. The Instant Now method captures the current timestamp in UTC format with nanosecond precision. The truncated to chrono unit dot minutes rounds the instant down to the nearest whole minute, removing any second or nanosecond precision. So the instant Incident now captures a timestamp given in the first comment. After that, the program sleeps for a second one second that is causing a delay before capturing logout time. After the sleep delay, another instant now call is made capturing logout time like in the second comment. Then it applies the truncated to chrono unit dot minutes to the login and logout times. So this method truncates the seconds and nanoseconds in the login and logout time stamps. So after applying the truncate two method, the minute value of the login and logout time stamps will be 58. Also, the date and time information will not change anyway. So it's going to be the same for both. For this reason, the if condition evaluates to false and the else block will be executed. So the else block will print the can't logout message. Good job. Let's move on to the next question. Question number 20. This question is about the Java Date and Time API. Go ahead, pause the video. Try to solve it yourself. We'll share the answer with you on the next slide. All right. Your answer is correct. If you chose option A. Let's walk through it step by step. Duration dot 5000. The duration class represents time in seconds and nanoseconds. The duration dot of Millie's 5000 creates a duration of 5000 milliseconds, which is five seconds when printed. Duration follows ISO 8601 format, so it prints as RT five S 5000 milliseconds as seconds. Duration dot of seconds 60. The duration dot of second 60 creates a duration of 60s one minute when printed duration outputs it as RT one 60s equals one minute period. Dot of days six. The period class represents date based amounts like days, months, and years, not time. The period dot of days six represents six days and prints as p XD concatenating all printed values. It's going to be like this RT five rt one p XD. And this matches option B. All right, let's move on to the next question. Question 21. So this question is about Java records, a feature introduced in Java 14, in order to provide an immutable data carrier class. Just go ahead and pause the video. Try to solve it yourself. And of course, you'll be able to get the answer on the next slide when we share it with you. All right. Well guess what? Your answer is correct if you chose option E. But let's walk through it step by step. So record in Java is a special type of class designed for immutable data storage. I know you already know that, but remember all instance variables in a record are implicitly final, meaning they cannot be reassigned. The constructor parameters automatically become final instance variables. Now, the problem in the code is that the field Regno is declared inside the record body and assign a value of 100. Since record fields are immutable and explicit, initialization of an instance variable inside the body is not going to be allowed. This is what causes a compilation error. How do you fix the issue though? Well, you remember the issue arises because Regno or Regno is an instance variable, but records do not allow mutable instance variables. What do you do? If we were to make Regno static, it becomes a class variable instead of an instance variable, which is allowed in records. So making the Regno variable static allows the code to compile successfully. All right. So let's move on to the next one. Here we've got a question about Java enums and overriding the two string method. So why don't you pause the video, try to solve it yourself and we'll show you the answer on the next slide. All right. So here you see the answer is correct if you chose option C. But what we'll do is walk through it step by step. Forecast. Returns the ordinal position of the enum constant. Enum constants are indexed starting from zero and the order they are declared. The index of sunny is zero, cloudy is one, and rainy is two, so the forecast sunny will return zero. Nice. Now let's have a look at the second print method. The forecast dot value of cloudy dot to uppercase. Converts cloudy in lowercase to cloudy, all in caps. Then it finds the enum constant. Cloudy. But if you notice the two string method is overridden in the enum class. So the question becomes how does the two string method affect output? Normally an enum constant prints its name by default. For this question, the second print method must print cloudy all caps. However, in this case we have overridden the two string method inside the forecast enum and it returns snowy. So this means whenever an instance of forecast is printed, instead of printing the enum constants name. For example cloudy, it prints snowy. So the final output will be zero. Snowy. Now we can move on to the next question. Question 23. So this question is about Java streams and their operations. Yeah. So go ahead pause the video. Try to solve it yourself. And we'll share with you the answer on the next slide coming up. All right good job. So your answer is obviously correct if you chose option C. Let's walk through it step by step. Now the code here uses Java streams to perform various operations on a list of special days. And here's what each part of the code does. The list of method creates an immutable list of strings containing four special days. In the first print method, the all match method checks if all elements in the stream match the given predicate. So in this case, it checks if all elements are equal to labor, and since only one element is labour, this returns false. So the first output will be false. Now the second print method, the any match method checks if any element in the stream matches the given predicate. And here it checks if any element is equal to labor. And since labor is in the list, this returns true. So the second output will be true. Now what happens in the third print method? The none match method checks if no elements in the stream match the given predicate. It checks if no elements are equal to Halloween. Since Halloween is not in the list, this returns true. So the third output will also be true. And then in the last print method, the find first method returns an optional containing the first element of the stream. In this case, it returns optional new year. So when we combine these results, the output is false. True, true. Optional new year. So the correct answer is option C. All right. Let's move on to the next question. Question 24. So this question is all about Java garbage collection and object references. So pause the video. Try to solve it yourself and we'll see what the answer is on the next slide. Okay. So your answer is correct if you chose option D. But let's explain it step by step. So in the main method two objects are created, one referenced by t1 with a name t1 and another referenced by t2 with a name t2. Then t2 is assigned to t1. So the reference t1 is now pointing to the same object as t2. This means the object with the name t1 is no longer referenced by T1. And lastly, the null is assigned to the T1. Now T1 does not reference any object. Okay. So now let's have a look at the garbage collection eligibility. The object originally referenced by T1 with the name T1 is no longer reachable because T1 was reassigned to T2 and then set to NULL. This object is eligible for garbage collection. The object referenced by T2 with the name T2 is still reachable through T2. So that is not eligible for garbage collection. So on the program prints GC only the object previously referenced by T1 with the name T1 is eligible for garbage collection. Therefore, the correct statement is only one of the objects previously referenced by T1 as eligible for garbage collection. All right, let's move on to the next question. Question 25. This question is about method overloading and binary literals in Java. Now just pause the video, try to solve it yourself, and we'll have a look at the answer on the next slide. All right so your answer is correct. If you chose option A. Let's walk through those step by step. The main class has two overloaded print methods one that takes an int parameter and another that takes a long parameter. So the expression 0B1101 underscore 1010 is a binary literal in Java. It represents a binary number 11011010, which is equivalent to the decimal number 218. Now the main method, the new main dot print method is called the binary literal given in. The print method is interpreted as an int because it fits within the range of the int type. Now, since the given binary literal is an int, the print method that takes an int parameter is invoked. This method prints hello, and therefore the result of running this code is. Hello. All right, let's move on to the next question. Question 26. This question is about the string. So just pause the video. Try to solve it yourself and we'll share with you the answer on the next slide. So guess what your answer is correct. If you chose option A let's break it down step by step. S1 refers to a string in the string pool. S2 refers to a new string object created in the heap. S3 refers to the intern string, which is the same as S1. Because the intern method creates an exact copy of a string that is present in the heap memory and stores it in the string constant pool. So according to this info, S1 is equal to S2. Will return false because they reference different objects, but S1 is equal to S3. Will return true because S3 is the interned version of S2, which points to the same object as S1. So the output will be false and true. Very good. Let's move on to the next question. Question 27. So this question is about the enum class. Why don't you pause the video and try to solve it yourself. And we'll share with you the To answer on the next slide. So your answer is correct. If you chose option C. Why don't we break it down again step by step. So all enums in Java implicitly extend Java.lang enum and implement constable comparable and serializable. So dur instance of enum is true because direction is an enum. DUR instance of comparable is also true because enums implement comparable. All right, let's move on to the next question, which is question 28. So here's a question about the wrapper class. Pause the video. Try to solve it yourself, and we'll have a look at the answer together on the next slide. Well sure enough your answer is correct if you chose option B. But why is that? So Java caches integer values between -100 and 28 and positive 127 for values outside this range. New objects are created. Now this is actually really important. So the A is equal to B will return true because 100 is within the cached range. But the c is equal to d will return false because 200 is outside the cached range and new objects are created, so the output will be true and false. Cool. Let's move on to the next question. Question 29. Now here's a question about the conversion between a list and an array. So why don't you go ahead and pause this and try to solve it yourself. And we'll talk about the answer on the next slide. Yes, your answer is correct. If you chose option B. Why is that? Well, the method arrays dot as list. Returns a fixed size list backed by an array. Attempting to modify this list. For example, adding any elements results in an unsupported operation exception. Good. Let's move on to the next question. Question 30. So this question is about the aggregate operations. Go ahead, pause the video and try to solve it yourself. And we'll share the answer on the next slide. Yes indeed. Your answer is correct. If you chose option D because the intermediate operations produce another stream not the terminal operations. So option D is a wrong statement if you want to move on to the next question. Of course you do. Question 31. So this question is about java.io. Pause the video, try to solve it yourself, and we'll share the answer with you on the next slide. Yes, your answer is correct if you chose option C, but we want to break it down step by step. The path of non-existent dir creates a path object for a directory given as the parameter. However, this does not check whether the directory actually exists. Files which is used to traverse the directory tree starting from the given path. It returns a stream of path objects representing all files and directories under the specified path. See if the directory specified by the path does not exist. The file's walk will throw a no such file exception at runtime. This is because files Walk attempts to access a directory and if it doesn't, it cannot proceed. Since the directory non-existent, dir does not exist. The program will throw a no such file exemption. That's a type of I o exception at runtime, and this will result in a runtime exception. Why not other options you might be asking? Well, a prints all files in non-existent underscore dir is incorrect because the directory does not exist, so there are no files to print. B compilation error. Yeah, this is incorrect because the code is syntactically correct and will compile without errors. D run successfully, but there's no output. This is incorrect because the program does not silently fail. It'll throw an exception. All right, let's move on to the next one. Question 32. So here we've got a question about Java multithreading. So why don't you pause the video, try to solve it yourself, and we'll have a look at the answer on the next slide. And your answer is correct if you chose option A. But we'll have to explain it step by step just to make sure. So the code creates two threads t1 t2 which are responsible for printing A and B respectively t1 dot start starts. The first thread which prints a t1 dot join, ensures that the main thread waits for T1 to complete before proceeding. This means A will always be printed before moving to the next thread. T2 dot start starts. The second thread which prints b T2 dot join ensures that the main thread waits for T2 to complete before the program ends. This means B will always be printed after a. Now, since the join method is used, the threads execute in a synchronized manner, ensuring that A is printed before B. Therefore, the output will always be a b. The program will always print A followed by B due to the use of the join method. All right, we'll move on to the next question. We're looking at question 33. So this question is about Java switch expressions. So go ahead pause the video and try to solve it yourself. And we'll have a look at the answer on the next slide. And your answer is correct if you chose option D but we should talk about it step by step. The code uses a switch expression with multiple cases for the same value, which is not allowed in Java. I know you knew that specifically though. Here the value two appears in both case one two and case two three. So this results in a compilation error because Java does not allow duplicate case values in switch expressions. So the program will not compile due to the duplicate case value two in the switch expression. Excellent. Let's move on to the next question. We've got question 34. This question is about Java collections, specifically the linked HashMap class. So you know the drill. Pause the video. Try it out for yourself. We'll share the answer with you in the next slide. And yes, your answer is correct if you chose option B, but let's walk through it step by step. The linked hash map is initialized with three parameters. Initial capacity is for, the load factor is 0.75 and access order is true. The access order parameter, which is true means that the iteration order of the map will reflect the order in which entries were last accessed from least recently accessed to most recently accessed. So here's what happens. Step by step. Map dot put one adds the entry a with key one to the map. Map dot put two B adds the entry B with key two to the map. Map dot put three adds the entry C with key three to the map map. Get one. Accesses the entry with key one moving it to the end of the iteration order. Map dot get three. Accesses the entry with key three moving it to the end of the iteration order. So after these operations, the iteration order of the map is B, with key two least recently accessed A with key one accessed after b, c with key three the most recently accessed. Therefore, the output will be two equals b one equals a, three equals c. All right. We'll move on to the next question. Question 35. So this question is about Java enums. So go ahead pause the video. Try to solve it yourself. And we'll share the answer with you on the next slide. Well your answer is correct if you chose option B. So let's walk through it step by step. The enum color has three constants red, green, and blue. So each time an enum constant is initialized, the constructor of the enum is called. In this case, the constructor prints C. All right. But here's what happens step by step. When the color enum is loaded, all its constants red, green, and blue are initialized, and this means the constructor is called three times printing C each time. In the main method, color C1 equals color dot blue assigns the blue constant to C1. Since the constants are already initialized, no additional constructor calls are made. System dot dot print. Line C1 prints the value of C1, which is blue. Therefore, the output will be c c c blue. All right, let's move on to the next question then. Question number 36. So this question is about Graalvm native image and Java application deployment. You know the drill. Pause the video, try to solve it yourself, and we'll share the answer with you on the other slide. So your answer is correct. If you chose option C let's explain it though step by step. So first off we need to talk about the deployment in Java. Packaging and deployment in Java refers to the process of preparing a Java application for distribution and execution on different systems. So this includes organizing files, compiling code, packaging it into deployable formats such as Jar war or native executables, and setting up the environment to run the application smoothly. Packaging involves bundling Java class files, resources, and metadata into a single archive. The most common formats include Jar, the Java Archive files, and this is used for packaging Java applications, libraries and resources to create a jar. We use the command shown right here in front of you and to run a jar. This is the command we use. So to ensure security jars can be digitally signed using Jar Signer. The other packaging format is War Web Application Archive files, so this is used for packaging Java web applications servlets jsps for deployment on a server. So you can see the structure here in front of you on the screen. So War files are deployed to a Java Enterprise Edition application server like Tomcat, Wildfly or GlassFish. The other packaging format though is Eere Enterprise Archive files. This is used for deploying enterprise applications containing multiple war and Jar files. This is deployed on Java Enterprise Edition application servers. But now we've got to talk about what is Graalvm native image. Graalvm native image is a technology that allows Java applications to be compiled ahead of time Aot into a native executable. This native executable does not require a JVM or Java virtual machine to run, and this reduces startup time and memory footprint. It's actually particularly useful for microservices, serverless functions, and other scenarios where fast startup and low resource usage are critical. So I hope this brief review sets the stage for you. Let's have a look at the options again. So in option A it says Graalvm can compile Java applications ahead of time into a native executable reducing startup time. And yeah that's true. Graalvm native image feature compiles Java applications into native executables, which start much faster than traditional JVM based applications. Now in option B, it says a graalvm native image includes a subset of the JDK runtime and does not require a separate JVM to execute. Yeah, this is also true. The native image includes only the necessary parts of the JDK runtime, and it runs without needing a JVM. In option C, it says reflection and dynamic class loading are fully supported in native images without additional configuration. This is false. This is the incorrect statement. Reflection and dynamic class loading are not fully supported by default in native images. Developers need to provide additional configuration. For example using reflection configuration files in order to ensure these features work correctly in native images. And what about an option D? It says native images significantly reduce memory footprint compared to traditional JVM execution. Yep, true native images typically use less memory than running the same application on a JVM, because they eliminate the overhead of the JVM and include only the necessary parts of the application. And an option E it says the native image tool must be used separately from Javac to generate a native executable. And that is also true. To see. The Native Image tool is a separate tool from the Java compiler, and is used to compile Java bytecode into a native executable. So as a result, Graalvm native images are a powerful tool for optimizing Java applications, but they require additional configuration for features like reflection and dynamic class loading. All right. Now we can move on to the next question, which happens to be question 37. So here we have a question that is about the Jar manifest file and what is required to create an executable jar. So go ahead pause the video. Try to solve it yourself and we'll share the answer with you on the next slide. And sure enough, your answer is correct if you chose option C. Let's go through it step by step. So what is a jar manifest? Well, jar manifest is a special file manifest contained within a jar, the Java archive file. It provides metadata about the Jar file such as version information, dependencies, and the main class for executable jars. In order for a jar to be executable, it must specify the main class the entry point of the application in the manifest. So now let's have a look at the explanation of the options. We've got a main class. Yeah this is required. The main class attribute is mandatory for an executable Jar. It specifies the fully qualified name of the class containing the main method. For example Com.example main. B class path. Well, this is optional. The classpath attribute specifies additional libraries or resources required by the application. While useful, it's really not required for a jar to be executable. See permissions. Guess what? It's optional. The permissions attribute is used inside jars to specify security permissions. It's not required for an executable jar de created by again optional. The created by attribute specifies the tool or version used to create the jar. It's not required for an executable jar e manifest version. This is optional. The manifest version attribute specifies the version of the manifest file format, usually 1.0. While commonly included, it's really not required for an executable jar f code base. Yeah, it's optional. The code base attribute is used in signed jars to specify the origin of the code, so it's not required in an executable jar. All right, let's move on to the next question. Question 38. This question is about Java C++ classes and interfaces. So pause the video, try to solve it yourself, and we'll share the answer with you on the next slide. Yes my friends, your answer is correct. If you chose option D. So why don't we go through it step by step? The content interface is declared as sealed right, meaning it can only be extended or implemented by a specific set of classes or interfaces, in this case story and art. The permits clause specifies which classes or interface are allowed to extend or implement content. Any class or interface that extends or implements a sealed type must explicitly declare itself as final, sealed, or non-sealed if it's non-sealed. It can be freely extended or implemented by other classes or interfaces. Now let's analyze the options here. Option A both story and art are declared as regular interfaces. This is incorrect because they must explicitly declare themselves as final, sealed or non-sealed. Option B similar to option A, but with public access modifiers, but it's still incorrect for the very same reason. Option C stories declared as sealed, but art is declared as non sealed. Well, this is incorrect because art must be an interface as permitted by content, not a class option D. Both story and art are declared as Non-sealed interfaces. This is correct because they comply with the rules for extending a sealed interface. But we've also got option E. Story is declared as a Non-sealed interface, but art is declared as a class. So this is going to be incorrect because art must be an interface as permitted by content. Option F given code does not produce any compilation error. As a result, options A, B, C, and E do not comply with the rules for extending or implementing a sealed type. They either fail to declare themselves as final sealed or non sealed, or they incorrectly use a class instead of an interface. So the correct set of class definitions is option D, where both story and art are declared as non-sealed interfaces. So this complies with the rules for extending a sealed interface. All right. Let's move on to the next question. Question 39. This question is about Java inheritance and instance of pattern matching. Pause the video, try it yourself, and we'll share the answer with you on the next slide. Yes, indeed, your answer is correct. If you chose option D. Let's explain it step by step. So the class hierarchy goes like this. A is the parent class, B extends a, c extends B. And now let's look at the object creation a b object equals new b creates an object of the type b but referenced by a AC object equals new C creates a new object of type C, but referenced by A, and in the first statement, the Instanceof operator is used with pattern matching. It checks if C object is an instance of the class B, and assigns it to the variable V if true. Now, since C object is actually an instance of C, which is a subclass of B, the condition C object is an instance of B evaluates to true, and then v is assigned the value of C object. So, in the scope of this first if statement v, dot calls the mbH method of B printing MHB. So the first output will be MHB. Now let's look at the nested if the nested if checks if v, which is of the type b, is also an instance of c, since v is actually an instance of C, the condition v instance of c v1 elevates to true and v1 is assigned the value of v. So in the scope of the nested if statement v1, dot MC calls the MC method of c printing MC. So the second output will be m c. As a result, the program prints m'b followed by MC. Awesome. Now let's move on to the next question. Question number 40. This question is about Java object references and method behavior. You know what to do. Pause the video, try to solve it yourself, and we'll share the answer on the next flight? Yes, indeed. Your answer is correct. If you chose option A. Let's walk through it step by step. So person P equals new person Joe creates a person object with the name Joe. Okay. Check. Person P is called. But the reference p in the main method is not affected by the changes inside. Check person. Because Java is passed by value, the p inside check person is a copy of the reference, so setting it to null does not affect the original p in the main method. So system dot out dot print line p prints Joe because p still points to the original person object. After that p equals null. Sets the reference p in the main method to null. Then check person p is called again. This time P is null. So check person creates a new person object with the name Mary. However, this new object is not assigned to the p in the main method because Java is passed by value. So system dot out dot print line p prints null because p in main is still null. The program prints Joe and null. Good job. Let's move on to the next question. What have we got? Question 41. So this question is about Java collections and unmodifiable lists. You know what to do. Pause the video, try to solve it yourself, and we'll share the answer with you on the next slide. Yes, indeed. Your answer is correct. If you chose option C. Well, let's walk through it step by step. See what you got. List one. This is a mutable array list containing A and B after line seven and eight. List two is created as an unmodifiable view of list one using collections. Unmodifiable list. List one. This means list two cannot be modified directly, but it reflects changes made to list one. On line ten, list one dot add C. Add C to list one. List two is a view of list one. It also reflects this change. However, list two itself cannot be modified directly. For example, using list two dot add would throw an Unsupportedoperationexception because list two is an Unmodifiable list. So the first print method prints a, b, c. Because list one is mutable and was modified, and the second print method also prints a, b, c. Because list two is a view of list one and reflects the changes made to list one. Let's move on to the next question then. Question 42. So here we've got a question about Java streams and collectors. Pause the video. Try to solve it yourself, and we'll share with you the answer on the next slide. And yeah, your answer is correct. If you chose option B we can break it down though step by step. What have we got. Well here's the explanation. Creating map one int stream dot range creates a stream of indices from zero to the minimum size of the two lists for. In this case, dot boxed converts the int stream to a stream of type integer dot collect creates a map where the key is the currency symbol, for example usd, GBP. The value is the reciprocal of the exchange rate. For example, 1.0 divided by 1.3255 for GBP. So the resulting map one is USD equals 1.0 GBP equals 0.7 544E equals 0.8355 CNY equals six dot 418. Creating map two. Map one dot set dot stream creates a stream of the entries in map one dot sorted. Sorts the entries by the key. The currency symbol in ascending order. Dot collect collects the sorted entries into a new linked hash map which preserves the insertion order, so the resulting map two is CNY equals six dot 418 E equals 0.8355. GBP equals 0.75. 44 USD equals 1.0. Lastly, map 2.4 each. Prints the entries in the order they appear in map two with values rounded to two decimal places. So the final output will be just like option C. All right. You ready to move on to the next question. Here we go. Question number 43. This question is about Java exception handling and control flow. Go ahead pause the video. Try to solve it yourself. We'll share the answer with you on the next slide. Your answer is correct. If you chose option A. Let's walk through it step by step. Though the do A method is called first it prints a. The if false condition ensures that no exception is thrown in the do a method. The do b method is called. Next it prints B. The if true condition ensures that a file not found exception is thrown. So now let's handle this exception. The file not found exception is a subclass of Ioexception, so it's caught by the catch Ioexception block the catch block prints. See the return statement and the catch block exits the method, but the finally block is executed before the method exits the finally block prints D. The program prints A from the do method, then B from the do b method, then C from the catch block, and lastly D from the finally block. The f is not printed because the return statement in the catch block prevents the execution of the last print method. So the output will be a, b, c, d. All right. Let's move on to the next question. Question number 44 okay. So this question is about using local date time period and local date time formatter. The date and time were created using the local datetime class. Then a period was created using the period class. Then the period is subtracted from the created date and time and transferred back to the date time object. Then later on, this date and time were formatted and printed on the console. The short constant in the format style enum class is used as the format. The question asks us what the output of this code will be. So go ahead, pause the video, try to solve it yourself, and we'll wait for you on the next slide. And yes indeed, your answer is correct if you selected option A. So first of all, we can say that this code can be compiled successfully because all the codes are created correctly. See, the period we created represents one year, one month, and one day. Therefore, this period is subtracted from the date and time created in the first line in the main method. The new date will be the 11th day of the fifth month of 2017, and the time section is going to remain the same. Now, since the short format style is used in the question, all parameters will be displayed as numeric and also the time section will only consist of the hour and minute parameters. So the seconds are not going to be displayed. So in this case the correct answer is given in option A. All right. Thanks for playing along. We've got another question for you. Question 45. In this question, an object of the Stringbuilder class was created and then append delete, char at and delete methods were applied to this object. So the question is what will be the output of this code. Go ahead pause the video. Try to solve it yourself, but we'll see you on the other slide with the correct answer. And yes, your answer is correct. If you chose option F, how'd you do? So why doesn't this code compile? You got to notice that while creating the Stringbuilder object in line one, a string value is passed to the builder object. We cannot create the Stringbuilder object like this. We need to create a Stringbuilder object using the new keyword. Therefore, this code will not compile. If the builder object was created correctly, then the output of this code would be series2. Given an option B now. Yeah, this is just a warning. You really do need to pay attention to these kinds of questions. If you try to solve the question without noticing the error in the very first line, you're going to be wasting your time. All right, now that being said, let's move on to the next question. Question 46. So some statements are given in this question, and we're asked to tick the incorrect option from these statements. So go ahead and pause the video. Try to solve it yourself and we'll give you the answer on the next slide. All right. Your answer is correct. If you selected option E. Why don't we examine the options together one at a time. So the expression and option A is the writer and output stream classes are used to write data. While this is a correct statement, both classes are used to write data. The expression and option B is the reader and input stream classes are used to read data well, this is also a correct statement. Both classes are used to read data. What about the expression and option C, which is. The Bufferedreader class is a well-known subclass of the reader class. While we've also got a correct statement, the Bufferedreader class is a subclass of the reader class. The expression and option D goes like. The buffered writer class is a well-known subclass of the writer class. We've also got a correct statement here. The buffered writer class is a subclass of the writer class. The expression and option E is the string array. Reader class is a well-known subclass of the reader class. Here you've got your incorrect statement because there is no class called String array reader. There is a class called char array reader. Therefore, the incorrect expression is given an option E. Good. Let's move on to the next question. Question number 47. Now this question is about the switch case statement. Three variables of type int are given and one of them is controlled by the switch case. The question is what will be the output of this code. So pause the video, try to solve it yourself and we'll share the answer on the next slide. Yes, your answer is correct. If you chose option C, you remember this, right? Only constant expressions were allowed as a case parameter in the switch case statement. If variables A and B were not defined as final, we would get a compilation error on line four and line five. But once a variable is declared as final, it is now constant. Therefore, we can use A and B in the cases. Here the variable we are checking in the switch parenthesis is c and the value of c is nine. But since there is no such value in cases, the code in the default statement will work and hello will be printed to the console. In addition, since the break keyword is not used in default and case one, the program will continue to progress and the statements in case one and case two will be printed to the console. Therefore, the statement hello Java developers is printed on the console screen. All right, let's move on to the next question. We've got question 47. So this question is about Java type compatibility and autoboxing. So pause the video here and try to solve it for yourself. We'll share the answer on the next slide. And yes your answer is correct. If you chose options A and D. We'll explain why the list contains integer objects 11 1213, 14 in option A. Double D equals list dot get zero. This causes a compile time error because list dot get zero. Returns an integer wrapper, and an integer wrapper cannot be directly assigned to a double wrapper without explicit casting or conversion. In option B, double f equals list dot get zero. And this is valid because list dot get zero. Returns an integer wrapper which can be automatically unboxed to a double primitive in option C. Int I equals list get zero. This is valid because list get zero. Returns an integer wrapper, which can of course be automatically unboxed to an int primitive. In option D, short s equals list get zero. This is invalid because list dot get zero. Returns an integer wrapper, and an integer wrapper cannot be directly assigned to a short primitive. We should use the value of method of the short wrapper to convert it in option e. Long L equals list get zero. And this is valid because list dot get zero. Returns an integer wrapper which can be automatically unboxed to a long primitive option f double e double dot value of list dot get zero. This is valid because double dot value of. Can take an integer wrapper as an argument and convert it to a double wrapper. So as a result, the codes given in options A and D will cause a compile time error. All right, let's move on to the next question. Question number 49. Well this question is about lambda expressions and type inference in Java. Go ahead pause the video. Try to solve it yourself. And we'll share with you the answer on the next slide. Well yes indeed your answer is correct if you chose option B but we'll talk about it. First we got to remember the lambda expression rules. Lambda parameters can use var for type inference. Lambda parameters can also use explicit types. For example. Integer. The modifier final can be used with lambda parameters, but it must be consistent with the type declaration. Either explicit or inferred with var. So according to this information, we'll have a look at the options in order. The code given in option A invalid mixing explicit types integer with var in lambda parameters not allowed. All parameters must use either explicit types or var, but not a mix of both and. The code given in option B is valid. Both parameters use var for type inference, and the second parameter uses the final modifier. What about the code given in option C? Invalid? The type integer is not a valid type in Java, it should be integer. The first letter should be uppercase. Yeah, so the code given in option D is invalid. Valid. The second parameter, y, does not have a type declaration, neither explicit nor var. The code given in option E is invalid as well. The type integer is not a valid type in Java. It should be integer. The first letter should be uppercase. Now if this had been written correctly, the code in option E would also be a valid code. So only the code given in option B is valid. All right. So what do you say we move on to the next question. We've got question 50. This question is about Java I o and file reading. Pause the video. Try to solve it yourself. And we'll share the answer with you on the next slide. All right. So your answer is correct if you chose option D. But why is that. Well let's first understand the code. So the code uses a file reader to read from a file specified by file to path a char array. Characters is created to store the data read from the file. The try with resources statement ensures that the file reader is closed automatically after use to read data into the char array. You need to use the read method with the parameter char array of file reader. So now let's have a look at the options in order. The code given in option A is incorrect because reader read returns an int the number of characters read, not a char. The code given in option B is incorrect because file reader does not have a read line method. This method is available in buffered reader. The code given in option C is incorrect because characters is a char array and does not have a read method. The code given in option D is correct, so the read method with the parameter char array reads characters into the char array, so we can insert the code in the option d on line one. Well, what do you think, my friends? We have done a lot here. Have you noticed we solved together a test equivalent to a practice exam? I know it was really useful, but of course, in order to get a successful grade on the exam, I really do recommend that you don't settle for just these questions. Also, research some more and solve some different types of questions. It all revolves around this knowledge. I know you're going to do great. So good luck to all of you in all of your future exams.
