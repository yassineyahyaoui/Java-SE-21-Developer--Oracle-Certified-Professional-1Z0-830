# Module 03 -- Basics of Java

### Q: How do you use `print`, `println`, and `printf` with format specifiers (`%d`, `%s`, `%n`) and `String.format` to produce formatted output?

Use `print`, `println`, and `printf` with format specifiers (`%d`, `%s`, `%n`) and `String.format` to produce formatted output

### Q: Read input with `Scanner` and explain the difference between `next()` (single token, stops at space) and `nextLine()` (full line)?

Read input with `Scanner` and explain the difference between `next()` (single token, stops at space) and `nextLine()` (full line)

### Q: How do you all three comment types (single-line, multi-line, Javadoc) and use Javadoc to document methods with `@param` and `@return`?

Write all three comment types (single-line, multi-line, Javadoc) and use Javadoc to document methods with `@param` and `@return`

### Q: Recall the section roadmap: output, input, comments, variables, data types, numbers, type conversion?

Recall the section roadmap: output, input, comments, variables, data types, numbers, type conversion

### Q: Declare and initialize variables using correct naming rules (case-sensitive, starts with letter/`$`/`_`, no keywords) and follow camelCase convention?

Declare and initialize variables using correct naming rules (case-sensitive, starts with letter/`$`/`_`, no keywords) and follow camelCase convention

### Q: Distinguish between instance variables, static variables, and local variables and state where each is declared?

Distinguish between instance variables, static variables, and local variables and state where each is declared

### Q: List all 8 primitive types with their sizes and value ranges, and explain String immutability?

List all 8 primitive types with their sizes and value ranges, and explain String immutability

### Q: How do you numeric literals with suffixes (`L` for long, `F` for float) and underscores, and identify invalid underscore placements (start, end, next to decimal point)?

Write numeric literals with suffixes (`L` for long, `F` for float) and underscores, and identify invalid underscore placements (start, end, next to decimal point)

### Q: Convert between decimal and binary/octal/hex both manually and using `Integer.toBinaryString()`, `toOctalString()`, `toHexString()`?

Convert between decimal and binary/octal/hex both manually and using `Integer.toBinaryString()`, `toOctalString()`, `toHexString()`

### Q: How do you widening (automatic) and narrowing (explicit cast) type conversions and predict data loss (e.g., `double` -> `long` -> `byte`)?

Perform widening (automatic) and narrowing (explicit cast) type conversions and predict data loss (e.g., `double` -> `long` -> `byte`)

### Q: What is The second method?

The second method is the system dot out dot print l n, and the last method would be system.out.print f.

### Q: What is Well this?

Well this is a class in the Java.io package.

### Q: What is here the println?

So here the println is a method that displays the string inside quotes.

### Q: What is you see the result?

So you see the result is the same right.

### Q: What is And this time I will use percent s?

And this time I will use percent s is the format to format a simple string.

### Q: What is And this?

And this is a process done for system efficiency.

### Q: What is And you can see the result?

And you can see the result is the same.

### Q: What is a string?

So a string is a series of characters.

### Q: What is And now you can see the result?

And now you can see the result is the same.

### Q: What is First one?

First one is the single line comment.

### Q: What is Second is multi-line comment, but the last one?

Second is multi-line comment, but the last one is the documentation comment.

### Q: What is you see the output?

So you see the output is the same.

### Q: What is Here I write x?

Here I write x is the first parameter.

### Q: What is Here I'll write y?

Here I'll write y is the second parameter.

### Q: What is first, like I said, we're going to start with the qu...?

So first, like I said, we're going to start with the question what is a variable.

### Q: What is while declaring variables, please note that Java?

So while declaring variables, please note that Java is a case sensitive programming language, which means that the upper or lower case of letters in your Java programs matter.

### Q: What is Also, we cannot use Java keywords as a variable name...?

Also, we cannot use Java keywords as a variable name, so int break is invalid because break is a keyword.

### Q: What is Int do is also invalid because do?

Int do is also invalid because do is a keyword and int Public is invalid because public is a keyword.

### Q: What is The byte data type?

The byte data type is an eight bit signed two's complement integer.

### Q: What is The short data type?

The short data type is a 16 bit signed two's complement integer, similar to.

### Q: What is The int data type?

The int data type is a 32 bit signed two's complement integer, which has a minimum value of minus two to the 31st and a maximum value of two to the 31st minus one.

### Q: What is The long data type?

The long data type is a 64 bit two's complement integer.

### Q: What is The float data type?

The float data type is a single precision 32 bit floating point.

### Q: What is The double data type?

The double data type is a double precision 64 bit floating point for decimal values.

### Q: What is The char data type?

The char data type is a single 16 bit Unicode character.
