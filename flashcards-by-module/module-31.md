# Module 31 -- Mock Exam - Java SE 21 Developer Certification

### Q: Work through ~50 mock exam questions covering all 1Z0-830 topics: Java fundamentals/JVM, variable scope and shadowing, logical operators and short-circuiting, switch-case (duplicate labels = compile error), arrays (jagged, `IndexOutOfBoundsException`), for-each (loop variable is a copy)?

Work through ~50 mock exam questions covering all 1Z0-830 topics: Java fundamentals/JVM, variable scope and shadowing, logical operators and short-circuiting, switch-case (duplicate labels = compile error), arrays (jagged, `IndexOutOfBoundsException`), for-each (loop variable is a copy)

### Q: Answer exam questions on OOP: method overloading (exact match vs widening vs varargs, ambiguous calls), record classes, abstract classes (constructor order), interfaces (default/static methods, multiple inheritance), sealed classes (`permits`, `final`/`sealed`/`non-sealed`), `instanceof` pattern matching?

Answer exam questions on OOP: method overloading (exact match vs widening vs varargs, ambiguous calls), record classes, abstract classes (constructor order), interfaces (default/static methods, multiple inheritance), sealed classes (`permits`, `final`/`sealed`/`non-sealed`), `instanceof` pattern matching

### Q: Answer exam questions on APIs: streams (`allMatch`/`anyMatch`/`noneMatch`/`findFirst`), `ZonedDateTime`, collections (`LinkedHashMap`), strings, garbage collection, lambdas (`var` in parameters), I/O (`FileReader`, try-with-resources), threads, exceptions?

Answer exam questions on APIs: streams (`allMatch`/`anyMatch`/`noneMatch`/`findFirst`), `ZonedDateTime`, collections (`LinkedHashMap`), strings, garbage collection, lambdas (`var` in parameters), I/O (`FileReader`, try-with-resources), threads, exceptions

### Q: Score at least 68% (passing threshold), identify your top 3 weakest topic areas, and create a focused review plan for Days 41-42?

Score at least 68% (passing threshold), identify your top 3 weakest topic areas, and create a focused review plan for Days 41-42

### Q: What is If you chose option A for each loops does not modify...?

If you chose option A for each loops does not modify the original array C, the variable num is a copy of the array element, not a reference to the original value.

### Q: What is If you chose option a c Java prioritizes exact match...?

If you chose option a c Java prioritizes exact matches first, and the print method with two int parameters is an exact match.

### Q: What is For print five and ten, print integer nums?

For print five and ten, print integer nums is a var args method, which is considered only if no exact match gets found.

### Q: What is If you chose option D, see the print with parameters...?

If you chose option D, see the print with parameters five and ten is ambiguous because the print method with parameters int and double is a valid match because ten is int and INT can be widened to double.

### Q: What is If you have selected options B, F, G, and h, you see...?

If you have selected options B, F, G, and h, you see the operator in option A is the unary increment Operator.

### Q: What is The operator in option B?

The operator in option B is the equal to operator.

### Q: What is The operator and option C?

The operator and option C is the add and assignment operator not used with a boolean data type.

### Q: What is The operator and option D?

The operator and option D is the subtraction operator.

### Q: What is The operator and option E?

The operator and option E is the greater than or equal to operator.

### Q: What is The operator in option G?

The operator in option G is the conditional and operator.

### Q: What is The operator in option h?

The operator in option h is the logical complement operator.

### Q: What is And lastly, this dot x?

And lastly, this dot x refers to the static field, not the parameter x, because we haven't yet transferred the parameter x to the variable x of this class in the constructor.

### Q: What is All other methods are automatically considered publi...?

All other methods are automatically considered public, so there is an exception here.

### Q: What is However, it?

However, it is a valid operation for a thread to switch from waiting to running.

### Q: What is The waiting state?

The waiting state is a state in which a thread is completely stopped working, and cannot continue working without a lock being released, or a notification notify being received.

### Q: What is record in Java?

So record in Java is a special type of class designed for immutable data storage.

### Q: What is Well, you remember the issue arises because Regno or...?

Well, you remember the issue arises because Regno or Regno is an instance variable, but records do not allow mutable instance variables.

### Q: What is the expression 0B1101 underscore 1010?

So the expression 0B1101 underscore 1010 is a binary literal in Java.

### Q: What is since the given binary literal?

Now, since the given binary literal is an int, the print method that takes an int parameter is invoked.

### Q: What is Will return true because S3?

Will return true because S3 is the interned version of S2, which points to the same object as S1.

### Q: What is dur instance of enum is true because direction?

So dur instance of enum is true because direction is an enum.

### Q: What is option D?

So option D is a wrong statement if you want to move on to the next question.

### Q: What is Packaging and deployment in Java?

Packaging and deployment in Java refers to the process of preparing a Java application for distribution and execution on different systems.

### Q: What is Graalvm native image?

Graalvm native image is a technology that allows Java applications to be compiled ahead of time Aot into a native executable.

### Q: What is The Native Image tool?

The Native Image tool is a separate tool from the Java compiler, and is used to compile Java bytecode into a native executable.
