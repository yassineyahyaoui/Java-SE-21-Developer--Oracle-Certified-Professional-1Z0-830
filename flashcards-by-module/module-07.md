# Module 07 -- Methods

### Q: What is a?

Define a method with access modifier, optional specifier (`static`, `final`, `abstract`), return type, name, and parameters

### Q: What is when a method runs (only when called)?

Explain when a method runs (only when called) and when it ends (last statement or `return`)

### Q: Distinguish standard library methods (e.g., `Math.min`, `Math.pow`) from user-defined methods and write methods returning void, primitives, or objects?

Distinguish standard library methods (e.g., `Math.min`, `Math.pow`) from user-defined methods and write methods returning void, primitives, or objects

### Q: How do you var-args methods using `...` syntax and state the rules: must be the last parameter, only one per method, treated as an array inside the method, can accept zero arguments?

Write var-args methods using `...` syntax and state the rules: must be the last parameter, only one per method, treated as an array inside the method, can accept zero arguments

### Q: How do you overloaded methods (same name, different parameter types or count) and predict which overload the compiler selects; identify that same name + same parameters = compile error?

Write overloaded methods (same name, different parameter types or count) and predict which overload the compiler selects; identify that same name + same parameters = compile error

### Q: How do you use `Math.pow()` to compute exponential values and cast the `double` result to `int`?

Use `Math.pow()` to compute exponential values and cast the `double` result to `int`

### Q: What is But basically the method?

But basically the method is a set of statements to perform an operation.

### Q: What is The private keyword?

The private keyword is an access modifier.

### Q: What is The return type?

The return type is a data type of the value that is returned by the method.

### Q: What is a parameter?

Now a parameter is a value passed to the method.

### Q: What is And the method name is test and this?

And the method name is test and this is a parameter list method.

### Q: What is If you notice this method?

If you notice this method is a void method so it doesn't return any value.

### Q: What is It's also a valid usage, but this return keyword is ...?

It's also a valid usage, but this return keyword is redundant because this is a void method.

### Q: What is But you can capitalize the first letter private beca...?

But you can capitalize the first letter private because Java is a case sensitive language.

### Q: What is When you select your preferences, the coffee machine...?

When you select your preferences, the coffee machine takes those inputs, that is the parameters and makes your coffee exactly the way you like it.

### Q: What is The print line method?

The print line method is a standard library method.

### Q: What is For example, the add number method that we defined i...?

For example, the add number method that we defined in the previous slide is an example of user defined methods.

### Q: What is For example the print method we use?

For example the print method we use is a standard library method.

### Q: What is in fact, hopefully you can understand here that var ...?

So in fact, hopefully you can understand here that var args is an array.

### Q: What is If the element in the loop?

If the element in the loop is the last element of the array, we just need to write that number.

### Q: What is the POW method?

So the POW method is a standard library method and it's defined in the math class.
