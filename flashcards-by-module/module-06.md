# Module 06 -- Control Flow Statements

### Q: How do you `if`, `if-else`, `if-else-if` ladder, and nested `if` statements and predict which branch executes?

Write `if`, `if-else`, `if-else-if` ladder, and nested `if` statements and predict which branch executes

### Q: What is single-line `if` rules (curly braces optional for one statement only)?

Explain single-line `if` rules (curly braces optional for one statement only) and the common trap where the second line always runs

### Q: How do you use the ternary operator (`?:`) as shorthand for `if-else`, and predict which branch executes and its side effects?

Use the ternary operator (`?:`) as shorthand for `if-else`, and predict which branch executes and its side effects

### Q: How do you `switch-case` statements and list the allowed types (byte, short, int, long, String, enum)?

Write `switch-case` statements and list the allowed types (byte, short, int, long, String, enum)

### Q: Predict output with and without `break` (fall-through behavior) and group multiple cases sharing one block?

Predict output with and without `break` (fall-through behavior) and group multiple cases sharing one block

### Q: What is the difference between `return` (exits the method)?

Explain the difference between `return` (exits the method) and `break` (exits only the switch) inside a case block

### Q: How do you `for` loops with init/condition/update, declare multiple variables in the init section, and explain loop variable scope?

Write `for` loops with init/condition/update, declare multiple variables in the init section, and explain loop variable scope

### Q: How do you use nested `for` loops and count total iterations; write infinite loops with `for(;;)`?

Use nested `for` loops and count total iterations; write infinite loops with `for(;;)`

### Q: How do you enhanced `for-each` loops (`for (Type item : array)`) and explain the limitation (no index access); use `toCharArray()` to iterate a String?

Write enhanced `for-each` loops (`for (Type item : array)`) and explain the limitation (no index access); use `toCharArray()` to iterate a String

### Q: How do you `while` loops (condition checked first) and implement a factorial program?

Write `while` loops (condition checked first) and implement a factorial program

### Q: Create `while(true)` infinite loops and predict unreachable-code compile errors (with literals, variables, and `final` constants)?

Create `while(true)` infinite loops and predict unreachable-code compile errors (with literals, variables, and `final` constants)

### Q: How do you `do-while` loops (body executes at least once) and implement a "continue or exit" user-input pattern?

Write `do-while` loops (body executes at least once) and implement a "continue or exit" user-input pattern

### Q: How do you use `break` to exit a loop or switch and state where it is valid (only inside loops/switch -- compile error otherwise)?

Use `break` to exit a loop or switch and state where it is valid (only inside loops/switch -- compile error otherwise)

### Q: How do you use `continue` to skip the current iteration and identify the infinite-loop risk when `continue` bypasses the increment?

Use `continue` to skip the current iteration and identify the infinite-loop risk when `continue` bypasses the increment

### Q: How do you use `return` to exit a method entirely and predict unreachable-code compile errors for statements after `return`?

Use `return` to exit a method entirely and predict unreachable-code compile errors for statements after `return`

### Q: How do you labeled loops (`labelName:` before loop) and use `break label` / `continue label` to control which loop in a nested structure is affected?

Write labeled loops (`labelName:` before loop) and use `break label` / `continue label` to control which loop in a nested structure is affected

### Q: Implement the Fibonacci series using a `for` loop with variable swapping?

Implement the Fibonacci series using a `for` loop with variable swapping

### Q: What is For example, simply typing control here?

For example, simply typing control here is the same as typing control is equal to true.

### Q: What is each if condition?

So each if condition is a block on its own.

### Q: What is But here we just created the if condition and each if?

But here we just created the if condition and each if is a block by itself.

### Q: What is And this?

And this is the decrement operator, and it's used as a suffix, not a prefix.

### Q: What is It's also one of the new features added in Java seven?

It's also one of the new features added in Java seven is the capability to switch on a string.

### Q: What is Case condition three is executed and day is Wednesday?

Case condition three is executed and day is Wednesday is the message that we have displayed.

### Q: What is And the default part of the switch statement is exec...?

And the default part of the switch statement is executed and invalid date choice is the message we have displayed.

### Q: What is And that, my friends?

And that, my friends, is the difference.

### Q: What is Okay, if there?

Okay, if there is a situation where we want it to give the same result in more than one situation, it might actually just be more logical not to use the default keyword here.

### Q: What is And the result?

And the result is the weekend.

### Q: What is The for loop?

The for loop is a control flow statement that iterates a part of the program multiple times.

### Q: What is The condition?

The condition is a Boolean expression.

### Q: What is As you can see the result?

As you can see the result is the same.

### Q: What is See how the result?

See how the result is the same.

### Q: What is since the operator of the condition?

So since the operator of the condition is the and operator, the result value of this condition is false and the loop is therefore terminated.

### Q: What is I wonder also, did you notice the type of a and b?

Now I wonder also, did you notice the type of a and b is the same?

### Q: What is we can say that it?

So we can say that it is an enhanced form of the for loop.

### Q: What is The first element?

The first element is a single item from the array, and the second item is the list of an array.
