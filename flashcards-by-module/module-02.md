# Module 02 -- Introduction to Java and Environment Setup

### Q: Describe the 1Z0-830 exam structure (50 questions, 120 min, 68% to pass) and the topics the course will cover?

Describe the 1Z0-830 exam structure (50 questions, 120 min, 68% to pass) and the topics the course will cover

### Q: How do you use `System.out.print`, `System.out.println`, and `System.out.printf` with format specifiers (`%d`, `%s`, `%n`)?

Use `System.out.print`, `System.out.println`, and `System.out.printf` with format specifiers (`%d`, `%s`, `%n`)

### Q: Read user input with the `Scanner` class using `nextInt()`, `next()`, and `nextLine()`, and explain when to call `close()`?

Read user input with the `Scanner` class using `nextInt()`, `next()`, and `nextLine()`, and explain when to call `close()`

### Q: How do you single-line (`//`), multi-line (`/* */`), and Javadoc (`/** */`) comments and explain their purpose?

Write single-line (`//`), multi-line (`/* */`), and Javadoc (`/** */`) comments and explain their purpose

### Q: What is the Java platform concepts: JDK vs JRE vs JVM?

Explain the Java platform concepts: JDK vs JRE vs JVM, bytecode, and "write once, run anywhere"

### Q: How do you widening (automatic) and narrowing (explicit) type conversions and state the size order: `byte` < `short` < `int` < `long` < `float` < `double`?

Perform widening (automatic) and narrowing (explicit) type conversions and state the size order: `byte` < `short` < `int` < `long` < `float` < `double`

### Q: Predict data loss when narrowing (e.g., `double` to `long` to `byte`) and fix compiler errors with explicit casts?

Predict data loss when narrowing (e.g., `double` to `long` to `byte`) and fix compiler errors with explicit casts

### Q: Set up IntelliJ IDEA Community, create a Java project with JDK 21, and navigate the IDE (project tree, editor, Run/Debug)?

Set up IntelliJ IDEA Community, create a Java project with JDK 21, and navigate the IDE (project tree, editor, Run/Debug)

### Q: Write, compile, and run a Hello World program; explain the flow: `.java` -> `javac` -> `.class` (bytecode) -> JVM -> output?

Write, compile, and run a Hello World program; explain the flow: `.java` -> `javac` -> `.class` (bytecode) -> JVM -> output

### Q: What is every part of `public static void main(String[] args)` including access modifier?

Explain every part of `public static void main(String[] args)` including access modifier, `static`, return type, and command-line arguments

### Q: What is And there?

And there is an exam that you've got to pass to get this certification.

### Q: What is The Java SE 21 Developer Professional Exam?

The Java SE 21 Developer Professional Exam is a multiple choice exam.

### Q: What is A package?

A package is an organized module of related interfaces and classes.

### Q: What is in object oriented programming, a class?

So in object oriented programming, a class refers to a set of related objects with common properties, classes, and the ability to create new classes.

### Q: What is Object oriented programming?

Object oriented programming is a powerful and flexible programming model.

### Q: What is an object?

Now, an object is a combination of related variables, constant and other data structures that can be selected and manipulated together.

### Q: What is the argument or arg?

So the argument or arg is a value that is passed into a command or a function.

### Q: What is for example, if calculate area?

So for example, if calculate area is a method that returns the area of a rectangle, then calculate area width three.

### Q: What is in addition, the Java programming language?

Now, in addition, the Java programming language is an object oriented language.

### Q: What is in Java, everything?

So in Java, everything is an object.

### Q: What is the general purpose high level Java programming lang...?

So the general purpose high level Java programming language is a very powerful software platform.

### Q: What is the Java Virtual Machine?

Now, the Java Virtual Machine is a virtual machine that runs Java programs.

### Q: What is And as you can see there?

And as you can see there is a Java folder.

### Q: What is note that this?

So note that this is the ultimate version of IntelliJ Idea right now.

### Q: What is Another option?

Another option is the Update Context menu.

### Q: What is First menu?

First menu is a projects menu.

### Q: What is The other menu?

The other menu is the customize menu.

### Q: What is Okay, so let's have a look at the next menu, which?

Okay, so let's have a look at the next menu, which is the plugin menu.

### Q: What is Anyway, this?

Anyway, this is a quick rundown of what you got in settings.

### Q: What is finally there?

Now finally there is a section here called Advanced Settings.

### Q: What is The section on the left?

The section on the left is a section where the project files are located.

### Q: What is The second part?

The second part is the part where we do the coding.

### Q: What is All right so this?

All right so this is the window where console outputs get displayed.

### Q: What is But if you notice there?

But if you notice there is a play icon and a bug icon here.

### Q: What is And just make sure that the page?

And just make sure that the page is the official web page where you can download IntelliJ Idea.
