# Module 04 -- Operators

### Q: Distinguish between operators and operands and identify the result type of arithmetic expressions?

Distinguish between operators and operands and identify the result type of arithmetic expressions

### Q: How do you use all five arithmetic operators (`+`, `-`, `*`, `/`, `%`) and explain `+` for string concatenation vs numeric addition?

Use all five arithmetic operators (`+`, `-`, `*`, `/`, `%`) and explain `+` for string concatenation vs numeric addition

### Q: How do you apply compound assignment operators (`+=`, `-=`, `*=`, `/=`, `%=`) and trace variable values step by step?

Apply compound assignment operators (`+=`, `-=`, `*=`, `/=`, `%=`) and trace variable values step by step

### Q: How do you use unary operators (`+`, `-`, `++`, `--`, `!`) and predict output differences between prefix (`++x`) and postfix (`x++`)?

Use unary operators (`+`, `-`, `++`, `--`, `!`) and predict output differences between prefix (`++x`) and postfix (`x++`)

### Q: Evaluate equality (`==`, `!=`) and relational (`>`, `<`, `>=`, `<=`) expressions and state that the result is always `boolean`?

Evaluate equality (`==`, `!=`) and relational (`>`, `<`, `>=`, `<=`) expressions and state that the result is always `boolean`

### Q: How do you use short-circuit `&&` (AND) and `||` (OR) and predict which operand is skipped when the result is already determined?

Use short-circuit `&&` (AND) and `||` (OR) and predict which operand is skipped when the result is already determined

### Q: How do you ternary expressions (`condition ? valueIfTrue : valueIfFalse`) as shorthand for `if/else`?

Write ternary expressions (`condition ? valueIfTrue : valueIfFalse`) as shorthand for `if/else`

### Q: How do you apply bitwise (`&`, `|`, `^`, `~`) and bitshift (`<<`, `>>`) operators on integers and verify results with `Integer.toBinaryString()`?

Apply bitwise (`&`, `|`, `^`, `~`) and bitshift (`<<`, `>>`) operators on integers and verify results with `Integer.toBinaryString()`

### Q: What is the `char` data type as an integer (0-65535)?

Explain the `char` data type as an integer (0-65535), perform `char` arithmetic (e.g., `'A' + 3`), cast between `char` and `int`, and use `Character.isLetter()`/`isDigit()`

### Q: Evaluate complex expressions by hand using the operator precedence table (multiplicative before additive, unary before binary) and use parentheses to override order?

Evaluate complex expressions by hand using the operator precedence table (multiplicative before additive, unary before binary) and use parentheses to override order

### Q: Distinguish between expressions, statements (end with `;`), and blocks (`{ }`)?

Distinguish between expressions, statements (end with `;`), and blocks (`{ }`)

### Q: What is For example, in the expression five plus four, the f...?

For example, in the expression five plus four, the five and the four are both operands, and the plus sign is an operator.

### Q: What is Asterisk?

Asterisk is a multiplication operator.

### Q: What is Forward slash?

Forward slash is a division operator.

### Q: What is equal to?

So equal to is a simple assign operator.

### Q: What is Plus and equal?

Plus and equal is an add and assignment operator.

### Q: What is Asterisk and equal?

Asterisk and equal is a multiply and assignment operator, so it multiplies the right operand with the left operand and then assigns the result to the left operand.

### Q: What is The forward slash and the equal?

The forward slash and the equal is a divide and assignment operator.

### Q: What is now this?

So now this is an ad, an assignment operator.

### Q: What is And then this?

And then this is a subtract and assignment operator.

### Q: What is And now we have the multiply and assignment operator...?

And now we have the multiply and assignment operator which is the same as z equals z multiply x.

### Q: What is z equals z minus x?

So z equals z minus x is the with 55 -20.

### Q: What is Multiply x?

Multiply x is the same with 35 star asterisk 20 times right.

### Q: What is Divide x?

Divide x is the same as 700 forward slash 20.

### Q: What is Plus this?

Plus this is a unary plus operator.

### Q: What is However, there?

However, there is a significant difference when using increment and decrement operators as prefix and suffix.

### Q: What is The second operator?

The second operator is the bitwise exclusive or operator.

### Q: What is The next operator we have here?

The next operator we have here is the bitwise exclusive or operator and it's represented by a carrot.

### Q: What is The next operator?

The next operator is the bitwise complement operator.

### Q: What is the last operator we have here?

So the last operator we have here is the signed right shift operator.

### Q: What is Again, I mean, that?

Again, I mean, that is a good example, don't get me wrong, but you'll see what I mean in a second.

### Q: What is Okay Java?

Okay Java is a powerful language.

### Q: What is But there really?

But there really is a subject that I need to.

### Q: What is let's see if the value stored by this variable?

Now let's see if the value stored by this variable is a letter or a number.

### Q: What is to understand whether variable?

Now to understand whether variable is a number or a letter we will use the is letter and is digit methods.
