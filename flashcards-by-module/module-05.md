# Module 05 -- Array

### Q: Declare arrays using both `type[] name` and `type name[]` syntax, instantiate with `new`, initialize with `{ }` literals, and state default values for unassigned elements (`0` for numeric, `null` for reference)?

Declare arrays using both `type[] name` and `type name[]` syntax, instantiate with `new`, initialize with `{ }` literals, and state default values for unassigned elements (`0` for numeric, `null` for reference)

### Q: Access elements by zero-based index, use `array.length`, and explain `ArrayIndexOutOfBoundsException`?

Access elements by zero-based index, use `array.length`, and explain `ArrayIndexOutOfBoundsException`

### Q: Sort arrays with `Arrays.sort()` (lexicographic for `String`, ASCII for `char`) and search sorted arrays with `Arrays.binarySearch()` (interpret the negative return value `-(insertion point) - 1`)?

Sort arrays with `Arrays.sort()` (lexicographic for `String`, ASCII for `char`) and search sorted arrays with `Arrays.binarySearch()` (interpret the negative return value `-(insertion point) - 1`)

### Q: Create 2D and 3D arrays and work with jagged arrays (rows of different lengths)?

Create 2D and 3D arrays and work with jagged arrays (rows of different lengths)

### Q: What is why wrapper classes exist (collections require objects)?

Explain why wrapper classes exist (collections require objects), convert between primitives and wrappers using `valueOf()` and `intValue()`/`doubleValue()`

### Q: What is autoboxing?

Define autoboxing (primitive -> wrapper) and unboxing (wrapper -> primitive) and predict `NullPointerException` when unboxing a `null` wrapper

### Q: What is if you are ready, which I know you are, come on let'...?

So if you are ready, which I know you are, come on let's get started with what is an array.

### Q: What is in Java an array?

So in Java an array is an object right.

### Q: What is in short an array?

So in short an array is a list of elements of the same type.

### Q: What is the array name?

So the array name is an identifier like a variable in the array declaration example you'll see here in front of you.

### Q: What is The number array?

The number array is an int array.

### Q: What is The first line on the console?

The first line on the console is the result of the addition operation.

### Q: What is The second line?

The second line is the array itself.

### Q: What is if your array?

So if your array is an int array an integer array, this value will be zero.

### Q: What is If the type of array?

If the type of array is a string, then this value is assigned as null.

### Q: What is If the type of the array you create?

If the type of the array you create is the object class, it is possible to create an array containing different data types.

### Q: What is See the object class?

See the object class is a class at the top of the class hierarchy in Java.

### Q: What is Since this element?

Since this element is a single digit, it has no equivalent in the second digit.

### Q: What is The second?

The second is the element for which we seek the index and that is eight.

### Q: What is after applying this the output will be four, which?

Now after applying this the output will be four, which is the index number of the number eight.

### Q: What is If yes, then inquire?

If yes, then inquire is the sort element within the array.

### Q: What is did you notice this?

Now did you notice this is a sorted array.

### Q: What is The first parameter?

The first parameter is the name of the array.

### Q: What is And since the element that we are looking for?

And since the element that we are looking for is an element of the sorted array, the output will be the index number of element eight.

### Q: What is And since the element we are looking for?

And since the element we are looking for is an element of the sorted array, the output will be the index number of element four.

### Q: What is Did you notice the array?

Did you notice the array is a sorted array?

### Q: What is the array?

So the array is a sorted array.
