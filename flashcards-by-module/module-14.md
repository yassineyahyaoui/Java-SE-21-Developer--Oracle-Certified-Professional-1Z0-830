# Module 14 -- Enum Types

### Q: Declare enums as predefined constant sets with UPPERCASE naming and use them in `switch` statements; compare enums with `==`?

Declare enums as predefined constant sets with UPPERCASE naming and use them in `switch` statements; compare enums with `==`

### Q: How do you use `valueOf(String)` to convert a string to an enum constant and handle `IllegalArgumentException` for invalid input?

Use `valueOf(String)` to convert a string to an enum constant and handle `IllegalArgumentException` for invalid input

### Q: What is enums?

Define enums with custom values via a private constructor, write a getter to expose the value, and iterate all constants with `values()`

### Q: Distinguish `name()` (final, returns constant name) from `toString()` (overridable)?

Distinguish `name()` (final, returns constant name) from `toString()` (overridable)

### Q: Combine all OOP concepts (inheritance, sealed types, records, enums, interfaces, polymorphism, encapsulation) in a mini-project?

Combine all OOP concepts (inheritance, sealed types, records, enums, interfaces, polymorphism, encapsulation) in a mini-project

### Q: What is an enum type?

So an enum type is a special data type that allows a variable to be a predefined set of constants okay.

### Q: What is In this part, the conditional switch expression?

In this part, the conditional switch expression is the instance of enum color.
